<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/home"
        android:icon="@drawable/baseline_home"
        android:title="home"
        app:showAsAction="ifRoom"
        />

    <item android:id="@+id/contactos"
        android:icon="@drawable/baseline_contactos"
        android:title="contaactos"
        app:showAsAction="ifRoom"
        />
    <item android:id="@+id/busqueda"
        android:icon="@drawable/baseline_search"
        android:title="busqueda"
        app:showAsAction="ifRoom"
        />

</menu>
